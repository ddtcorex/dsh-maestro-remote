name: CI

on:
  push:
    branches: [master]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    uses: ddtcorex/dsh-maestro-ci/.github/workflows/node-plugin.yml@625ccffb5519ff6a8bf0d3059489e1915a8d85eb
    with:
      pnpm-version: ""
      sibling-repos: "dsh-maestro-config-lib@master"
